# CHIP AI 前后端联调交付文档

## 📋 项目概述

本文档基于现有Spring Boot项目架构，为后端工程师提供新前端页面的集成指南。项目包含两个新的前端页面，需要与现有后端系统进行集成：

1. **landing.html** - 现代化产品介绍落地页
2. **demo.html** - 全新智能芯片Pin2Pin分析工作台

## 🏗️ 现有项目架构

### 技术栈
- **后端**: Spring Boot 3.4.x + MyBatis + MySQL
- **前端**: 原生HTML5 + CSS3 + JavaScript (ES6+)
- **模板引擎**: Thymeleaf
- **AI集成**: DeepSeek API / GPT-4o
- **文件处理**: Apache PDFBox
- **支付**: 支付宝API

### 现有项目结构
```
AI-P2P-master/
├── demo.html              # 新增：主工作台页面
├── landing.html           # 新增：产品落地页
├── src/main/java/         # Spring Boot后端代码
│   └── com/babata/chip/
│       ├── controller/    # 页面控制器
│       ├── controller/rest/ # REST API控制器
│       ├── service/       # 业务服务层
│       ├── repository/    # 数据访问层
│       └── common/        # 公共组件
├── src/main/resources/    # 配置和模板
│   ├── templates/         # Thymeleaf模板
│   ├── static/           # 静态资源
│   └── application.yml   # 配置文件
└── files/                # 文件上传目录
```

### 现有后端接口概览
项目已具备完整的后端API体系：

#### 芯片相关API (`ChipRestController`)
- `GET /api/chip/search` - 芯片搜索
- `GET /api/chip/search/fuzzy` - 模糊搜索
- `GET /api/chip/detail` - 芯片详情
- `POST /api/chip/analyze` - 芯片分析（非流式）
- `POST /api/chip/analyze/stream` - 芯片分析（SSE流式）
- `GET /api/chip/compare/records` - 分析记录
- `GET /api/chip/compare/result` - 下载分析结果
- `POST /api/ai/session` - 创建AI会话

#### 文件上传API (`FileRestController`)
- `POST /api/chip/pdf/upload` - PDF文件上传
- `POST /api/file/album` - 图片上传（EditorMD）

#### 文章API (`ArticleRestController`)
- `POST /api/article/add` - 发布文章
- `GET /api/article/posts` - 获取文章列表
- `GET /api/article/{enTitle}` - 获取文章详情

## 🎯 新前端页面集成方案

### 1. 产品落地页 (landing.html)

#### 页面功能
- **导航栏**: 固定顶部，滚动时显示阴影效果
- **Hero区域**: 产品介绍和CTA按钮
- **特性展示**: 6个核心功能卡片
- **工作流程**: 4步使用说明
- **定价方案**: 3个价格套餐（免费/专业/企业）
- **技术博客**: 博客文章展示
- **CTA区域**: 最终转化区域
- **Footer**: 页脚信息和链接

#### 集成现有接口
```javascript
// 1. 博客文章列表 - 使用现有接口
GET /api/article/posts?page=1&pageSize=9
Response: {
  "success": true,
  "data": {
    "page": 1,
    "pageSize": 9,
    "total": 25,
    "list": [
      {
        "id": 1,
        "title": "如何利用AI加速芯片Pin2Pin替代分析",
        "enTitle": "ai-chip-pin2pin-analysis",
        "subtitle": "传统的芯片选型需要工程师手动对比...",
        "tag": "芯片分析",
        "coverImage": "https://images.unsplash.com/photo-1581092580497-e0d23cbdf1dc?q=80&w=1770",
        "author": "admin",
        "createTime": "2025-01-15T10:30:00Z",
        "readCount": 156,
        "contentMd": "文章内容摘要..."
      }
    ]
  }
}

// 2. 定价方案 - 可新增接口或硬编码
GET /api/pricing/plans (可选)
Response: {
  "success": true,
  "data": [
    {
      "id": "free",
      "name": "免费版",
      "price": 0,
      "period": "永久免费",
      "features": ["每日3次分析额度", "基础芯片参数对比", "Markdown报告导出", "7天历史记录"]
    },
    {
      "id": "professional",
      "name": "专业版",
      "price": 299,
      "period": "每月",
      "features": ["每日50次分析额度", "高级AI分析引擎", "多格式报告导出", "无限历史记录", "优先技术支持"],
      "popular": true
    },
    {
      "id": "enterprise",
      "name": "企业版",
      "price": null,
      "period": "按需报价",
      "features": ["无限分析次数", "私有化部署", "API接口对接", "定制化开发", "专属客户经理"]
    }
  ]
}
```

#### 需要添加的控制器方法
在 `IndexController` 中添加：
```java
@Controller
public class IndexController {
    // 现有方法...
    
    @RequestMapping("/landing")
    public String landing() {
        return "landing";
    }
    
    @RequestMapping("/demo")
    public String demo() {
        return "demo";
    }
}
```

### 2. 智能分析工作台 (demo.html)

#### 页面布局
```
┌─────────────────┬─────────────────────────────┐
│   左侧边栏      │        主内容区              │
│  ┌──────────┐   │  ┌───────────────────────┐  │
│  │上传区域  │   │  │    顶部导航栏          │  │
│  └──────────┘   │  └───────────────────────┘  │
│  ┌──────────┐   │  ┌───────────────────────┐  │
│  │文件列表  │   │  │    Tab导航            │  │
│  └──────────┘   │  └───────────────────────┘  │
│  ┌──────────┐   │  ┌───────────────────────┐  │
│  │设置区域  │   │  │    内容区域           │  │
│  └──────────┘   │  └───────────────────────┘  │
│                 │  ┌───────────────────────┐  │
│                 │  │    底部输入区          │  │
│                 │  └───────────────────────┘  │
└─────────────────┴─────────────────────────────┘
```

#### 集成现有接口

##### 2.1 文件上传模块
```javascript
// 使用现有PDF上传接口
POST /api/chip/pdf/upload
Content-Type: multipart/form-data
Body: FormData with PDF files
Response: {
  "success": true,
  "message": "文件上传成功"
}

// 注意：现有接口返回简单成功消息，前端需要适配
// 建议增强现有接口返回文件信息
```

**现有接口增强建议**：
```java
// 在 FileRestController.java 中增强 uploadPdfFile 方法
@PostMapping("/api/chip/pdf/upload")
public BaseResponse uploadPdfFile(@RequestParam("file") MultipartFile[] files) throws IOException {
    List<Map<String, Object>> fileInfoList = new ArrayList<>();
    
    for (MultipartFile file : files) {
        if (file.isEmpty()) {
            return buildErrorResponse("文件为空");
        }
        String fileType = FileUtil.getFileType(Objects.requireNonNull(file.getOriginalFilename()));
        if (!Objects.equals(fileType, "pdf")) {
            return buildErrorResponse("文件格式不合法");
        }

        // 生成唯一文件名避免冲突
        String originalFilename = file.getOriginalFilename();
        String uniqueFilename = UUID.randomUUID().toString() + "_" + originalFilename;
        File f = new File(System.getProperty("user.dir") + PDF_FILE_PATH + uniqueFilename);
        file.transferTo(f);
        
        // 返回文件信息
        Map<String, Object> fileInfo = new HashMap<>();
        fileInfo.put("id", uniqueFilename);
        fileInfo.put("name", originalFilename);
        fileInfo.put("size", file.getSize());
        fileInfo.put("uploadTime", new Date());
        fileInfoList.add(fileInfo);
    }
    return buildSuccessResponse(fileInfoList);
}
```

##### 2.2 芯片分析模块
```javascript
// 使用现有流式分析接口
POST /api/chip/analyze/stream
Body: {
  "fileList": ["file_123", "file_456"], // 文件名列表
  "additionalRequirements": "重点关注供电电压和封装兼容性" // 可选
}

// 流式响应 (SSE) - 现有接口已支持
data: {"type": "progress", "value": 25, "message": "正在提取PDF文本..."}
data: {"type": "content", "content": "# 芯片对比分析报告\n\n## 1. 基本信息"}
data: {"event": "analysis-complete", "data": "✅ 分析完成"}

// 分析历史记录 - 使用现有接口
GET /api/chip/compare/records
Response: {
  "success": true,
  "data": [
    {
      "id": 123,
      "firstChipPartNumber": "TLV2365-Q1",
      "secondChipPartNumber": "INA950-SEP",
      "createTime": "2025-01-20T10:30:00Z",
      "status": 1, // StatusEnum.VALID.getCode()
      "result": "分析结果内容..."
    }
  ]
}
```

##### 2.3 问答助手模块
```javascript
// 使用现有会话创建接口
POST /api/ai/session
Body: {
  "sessionTitle": "芯片替代分析咨询" // 可选，默认"新会话"
}
Response: {
  "success": true,
  "data": 12345 // sessionId
}

// 需要新增：聊天消息接口
POST /api/ai/chat
Body: {
  "sessionId": 12345,
  "message": "如何进行Pin2Pin芯片替代分析？"
}

// 需要新增：流式聊天响应
GET /api/ai/chat/stream/{sessionId}
Response: Server-Sent Events
data: {"type": "message", "content": "进行Pin2Pin芯片替代分析的步骤如下："}
data: {"type": "complete"}

// 需要新增：会话历史
GET /api/ai/sessions
Response: {
  "success": true,
  "data": [
    {
      "id": 12345,
      "title": "芯片替代分析咨询",
      "createTime": "2025-01-20T10:30:00Z",
      "messageCount": 5
    }
  ]
}
```

##### 2.4 博客发布模块
```javascript
// 使用现有文章发布接口
POST /api/article/add
Body: {
  "title": "如何利用AI加速芯片Pin2Pin替代分析",
  "enTitle": "ai-chip-pin2pin-analysis",
  "subtitle": "传统的芯片选型需要工程师手动对比...",
  "tag": "芯片分析",
  "coverImage": "", // 可选，有默认值
  "privacy": "public", // public | private
  "content": "# 博客内容\n\n..."
}

Response: {
  "success": true,
  "message": "发布成功"
}
```

## � 集成实施步骤

### 第一步：静态资源部署
1. **复制前端文件到静态资源目录**
```bash
# 将新前端页面复制到Spring Boot静态资源目录
cp demo.html src/main/resources/static/
cp landing.html src/main/resources/static/
```

2. **验证配置文件**
确认 `application.yml` 中的静态资源配置：
```yaml
spring:
  mvc:
    static-path-pattern: /static/**
```

### 第二步：添加页面路由
在 `IndexController.java` 中添加新的页面路由：
```java
@Controller
public class IndexController {
    // 现有方法保持不变...
    
    @RequestMapping("/")
    public String index() {
        return "index";
    }
    
    // 新增：产品落地页
    @RequestMapping("/landing")
    public String landing() {
        return "landing";
    }
    
    // 新增：智能分析工作台
    @RequestMapping("/demo")
    public String demo() {
        return "demo";
    }
}
```

### 第三步：增强现有接口（可选）

#### 3.1 增强文件上传接口
在 `FileRestController.java` 中修改 `uploadPdfFile` 方法：
```java
@PostMapping("/api/chip/pdf/upload")
public BaseResponse uploadPdfFile(@RequestParam("file") MultipartFile[] files) throws IOException {
    List<Map<String, Object>> fileInfoList = new ArrayList<>();
    
    for (MultipartFile file : files) {
        if (file.isEmpty()) {
            return buildErrorResponse("文件为空");
        }
        String fileType = FileUtil.getFileType(Objects.requireNonNull(file.getOriginalFilename()));
        if (!Objects.equals(fileType, "pdf")) {
            return buildErrorResponse("文件格式不合法");
        }

        // 生成唯一文件名避免冲突
        String originalFilename = file.getOriginalFilename();
        String uniqueFilename = UUID.randomUUID().toString() + "_" + originalFilename;
        File f = new File(System.getProperty("user.dir") + PDF_FILE_PATH + uniqueFilename);
        file.transferTo(f);
        
        // 返回文件信息
        Map<String, Object> fileInfo = new HashMap<>();
        fileInfo.put("id", uniqueFilename);
        fileInfo.put("name", originalFilename);
        fileInfo.put("size", file.getSize());
        fileInfo.put("uploadTime", new Date());
        fileInfoList.add(fileInfo);
    }
    return buildSuccessResponse(fileInfoList);
}
```

#### 3.2 新增AI聊天接口（可选）
创建 `AIRestController.java`：
```java
@RestController
public class AIRestController {
    
    @Resource
    private SessionRepository sessionRepository;
    
    @Resource
    private MessageRepository messageRepository;
    
    @PostMapping("/api/ai/chat")
    public BaseResponse chat(@RequestParam("sessionId") Long sessionId, 
                           @RequestParam("message") String message) {
        // 保存用户消息
        messageRepository.addMessage(sessionId, "user", message);
        
        // 调用AI服务获取回复
        String aiResponse = aiService.getResponse(message);
        
        // 保存AI回复
        messageRepository.addMessage(sessionId, "assistant", aiResponse);
        
        return buildSuccessResponse(aiResponse);
    }
    
    @GetMapping("/api/ai/chat/stream/{sessionId}")
    public SseEmitter chatStream(@PathVariable Long sessionId) {
        SseEmitter emitter = new SseEmitter(60_000L);
        // 实现流式聊天逻辑
        return emitter;
    }
    
    @GetMapping("/api/ai/sessions")
    public BaseResponse getSessions() {
        Integer userId = UserCache.getUserContext().getUserid();
        List<SessionDO> sessions = sessionRepository.getSessionsByUserId(userId);
        return buildSuccessResponse(sessions);
    }
}
```

### 第四步：前端适配调整

#### 4.1 修改demo.html中的API调用
```javascript
// 文件上传适配现有接口
async function uploadFiles(files) {
    const formData = new FormData();
    files.forEach(file => {
        formData.append('file', file);
    });
    
    const response = await fetch('/api/chip/pdf/upload', {
        method: 'POST',
        body: formData
    });
    
    const result = await response.json();
    if (result.success) {
        // 适配现有接口返回格式
        return result.data || [{ name: '上传成功' }];
    }
    throw new Error(result.message);
}

// 芯片分析适配现有接口
async function startAnalysis(fileList, additionalRequirements) {
    const response = await fetch('/api/chip/analyze/stream', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json'
        },
        body: JSON.stringify({
            fileList: fileList,
            additionalRequirements: additionalRequirements
        })
    });
    
    // 处理SSE流式响应
    const reader = response.body.getReader();
    const decoder = new TextDecoder();
    
    while (true) {
        const { done, value } = await reader.read();
        if (done) break;
        
        const chunk = decoder.decode(value);
        // 处理SSE数据格式
        processSSEData(chunk);
    }
}
```

#### 4.2 修改landing.html中的博客调用
```javascript
// 适配现有文章接口
async function loadBlogPosts() {
    const response = await fetch('/api/article/posts?page=1&pageSize=9');
    const result = await response.json();
    
    if (result.success) {
        // 适配现有数据结构
        return result.data.list || [];
    }
    return [];
}
```

## 🔌 现有接口规范

### 通用响应格式（已实现）
```javascript
// 成功响应
{
  "success": true,
  "data": {...},
  "message": "操作成功"
}

// 错误响应
{
  "success": false,
  "message": "错误信息"
}

// 分页响应
{
  "success": true,
  "data": {
    "page": 1,
    "pageSize": 9,
    "total": 25,
    "list": [...]
  }
}
```

### 现有错误处理
项目已实现完整的错误处理机制：
- `ErrorCodeEnum` - 错误码枚举
- `GlobalExceptionHandler` - 全局异常处理
- `ResponseUtil` - 响应工具类

### 认证机制（已实现）
- `UserCache` - 用户缓存
- `UserContext` - 用户上下文
- `LoginHandlerInterceptor` - 登录拦截器
- 基于Session的认证机制

## 🎨 前端组件说明

### UI组件系统
新前端页面基于Anthropic设计规范，包含：

#### 颜色系统
```css
:root {
  --bg-primary: #faf9f5;      /* 主背景色 */
  --bg-secondary: #e8e6dc;    /* 次背景色 */
  --text-primary: #141413;    /* 主文字色 */
  --accent-orange: #d97757;   /* 强调色-橙 */
  --accent-blue: #6a9bcc;     /* 强调色-蓝 */
  --accent-green: #788c5d;    /* 强调色-绿 */
}
```

#### 组件样式
- **按钮**: 主要/次要/文本按钮
- **表单**: 输入框/选择框/开关
- **卡片**: 功能卡片/博客卡片
- **模态框**: 发布对话框
- **Toast**: 成功/错误提示

### 交互效果
- **动画**: CSS3过渡和关键帧动画
- **响应式**: 移动端适配
- **无障碍**: 键盘导航支持

## 📱 响应式设计

### 断点设置
```css
/* 平板 */
@media (max-width: 1024px) { ... }

/* 手机 */
@media (max-width: 768px) { ... }

/* 小屏手机 */
@media (max-width: 480px) { ... }
```

### 适配要点
- **demo.html**: 侧边栏折叠，编辑器垂直布局
- **landing.html**: 导航栏简化，卡片单列显示

## 🔧 开发调试

### 本地开发环境
1. 启动Spring Boot应用:
```bash
./mvnw spring-boot:run
```

2. 访问地址:
- 应用主页: `http://localhost:80`
- 新落地页: `http://localhost:80/landing`
- 新工作台: `http://localhost:80/demo`

### 调试工具
- **浏览器控制台**: 查看API请求和错误
- **Network面板**: 监控接口调用
- **Console日志**: 前端调试信息

### 常见问题解决

#### 1. 静态资源404
- 检查文件是否在正确的静态资源目录
- 确认 `application.yml` 中的路径配置

#### 2. 接口跨域
- 现有项目已解决，新页面使用相同域名

#### 3. SSE连接问题
- 检查现有 `ChipRestController.analyzeChip` 方法
- 确认前端正确处理SSE数据格式

## 📊 性能优化建议

### 前端优化
- **图片懒加载**: 博客封面图延迟加载
- **代码分割**: 按需加载JavaScript
- **缓存策略**: 静态资源缓存

### 后端优化（现有）
- **文件处理**: 异步PDF解析（已实现）
- **AI调用**: 请求队列管理（已实现）
- **数据库**: 分析结果缓存（可优化）

## 🔒 安全考虑

### 现有安全机制
- **XSS防护**: Thymeleaf自动转义
- **CSRF防护**: 已实现Token验证
- **文件上传**: 类型和大小限制（已实现）
- **权限控制**: 基于角色的访问控制（已实现）

### 新页面安全建议
- **输入验证**: 前端表单验证
- **内容过滤**: 博客内容安全检查
- **文件限制**: PDF文件大小和格式验证

## 🚀 部署说明

### 静态资源部署
```bash
# 确保前端文件在正确位置
ls -la src/main/resources/static/
# 应包含: demo.html, landing.html
```

### 配置验证
确认现有配置文件包含：
```yaml
server:
  port: 80

spring:
  mvc:
    static-path-pattern: /**
  resources:
    static-locations: classpath:/static/

files:
  path:
    pdf-file-path: /files/pdf/
```

## 📞 联调支持

### 现有联系人
- **项目负责人**: [现有联系方式]
- **后端开发**: [现有联系方式]

### 测试环境
- **测试地址**: [现有测试环境URL]
- **测试账号**: [现有账号信息]

---

## 📝 集成检查清单

### ✅ 基础集成
- [ ] 复制demo.html和landing.html到static目录
- [ ] 在IndexController中添加页面路由
- [ ] 验证页面可正常访问

### ✅ 接口适配
- [ ] 测试文件上传接口兼容性
- [ ] 验证芯片分析SSE流式响应
- [ ] 确认博客文章列表接口正常

### ✅ 功能测试
- [ ] 文件拖拽上传功能
- [ ] 芯片分析流程完整性
- [ ] 博客发布功能
- [ ] 响应式布局适配

### ✅ 优化增强（可选）
- [ ] 增强文件上传返回详细信息
- [ ] 实现AI聊天功能
- [ ] 添加定价方案接口

---

**注意事项**:
1. 新前端页面基于现有项目架构设计，大部分功能可直接使用现有接口
2. 建议先进行基础集成，确保页面可访问，再逐步增强功能
3. 现有项目已具备完整的认证、权限、错误处理机制，新页面可直接复用
4. 如有问题，请参考现有接口实现或联系开发团队

祝集成顺利！🚀
